# Rust
target/
**/*.rs.bk
Cargo.lock.bak

# Yew / WASM
ui/dist/
ui/pkg/
ui/target/
ui/node_modules/

# Tauri
src-tauri/target/
src-tauri/gen/

# Build Windows (cross-compile, scripts/build.sh)
/dist-windows/

# Editor
.vscode/
.idea/
*.iml
*.swp
*.swo
.DS_Store
Thumbs.db

# Vault local (fora do repo, em ~/.anotadinho-vault ou pendrive)
/vault/
*.vault/

# Cycles - mantém tasks e status, ignora logs de execução
cycles/run-logs/

# Env
.env
.env.local

# Cache do indice do vault (ciclo 171) — e cache, nao fonte da verdade
.anotadinho/

# A página de rascunho da suíte de UI: criada e apagada pelos testes.
# Sem isto, um `git add -A` no meio de uma rodada commita o fixture do
# cenário que estava correndo.
VaultAnotadinho/pages/__uitest.md
