---
title: "Executar: Proposta de implementação — imagens persistidas e personalizáveis"
type: conversa
origem: pages/propostas/proposta-de-implementacao-imagens-persistidas-e-personalizaveis.md
contexto:
- pages/propostas/proposta-de-implementacao-imagens-persistidas-e-personalizaveis.md
tags:
- conversa
---
## você · 2026-08-28 23:33

Execute a proposta "Proposta de implementação — imagens persistidas e personalizáveis", que já foi revisada e aprovada.

Ao terminar, relate:

1. O que foi feito, etapa por etapa.
2. Como foi validado.
3. O que ficou de fora, e por quê.

Siga a abordagem aprovada. Se durante a execução ela se mostrar inviável, PARE e explique o problema em vez de mudar de rumo por conta — mudar a abordagem exige uma proposta nova.

## agente · 2026-08-28 23:44

A implementação está pronta no código, mas o ciclo 226 permanece `doing` porque a validação obrigatória de UI não pôde ser concluída.

1. O que foi feito

- Criado o ciclo 226 com critérios e não-objetivos.
- Implementado modelo semântico de imagem no core.
- Adicionada serialização HTML com `<figure>`, `<img>` e `<figcaption>`.
- Suporte a alt, título, legenda, largura, altura, alinhamento e proporção.
- Implementada gravação em lote com validação prévia, nomes sempre novos e limpeza em falhas.
- `/imagem` agora usa seletor nativo e abre o modal preenchido.
- Drop simples ou múltiplo abre o mesmo modal, preserva ordem e ignora não-imagens.
- Paste permanece direto, inclusive para múltiplas imagens.
- Inserções usam `Range`, sem `execCommand`.
- Posição assíncrona é preservada com `use_mut_ref` e revalidada antes da gravação.
- Cancelamento não grava assets.
- HTML reaberto preserva os metadados e resolve assets relativos sem persistir `blob:`.
- Adicionado alvo visual de drop, estilos BEM/tokens e navegação `data-nav-*`.
- Adicionados testes unitários e três cenários permanentes de harness.

2. Como foi validado

Passou:

- `cargo test --workspace`: 482 testes.
- `cargo check` da UI.
- `cargo build --manifest-path src-tauri/Cargo.toml`.
- Validação sintática do harness.
- `git diff --check`.
- Testes novos de serialização, validação, duplicidade intencional e lote inválido sem publicação.

Pendente:

- `trunk build`: o ambiente não possui o target `wasm32-unknown-unknown` e também não possui `rustup`.
- Harness: não havia app ouvindo em `127.0.0.1:9223`.

3. O que ficou de fora

- Inspeção visual do modal, alvo de drop e imagens renderizadas.
- Execução dos novos cenários contra o app real.
- Atualização do ciclo para `done`, status final e commit — proibidos pelas regras do projeto enquanto o harness estiver pendente.
- Nenhum snapshot foi atualizado sem inspeção visual.
- A spec e a proposta não tiveram suas etapas de fluxo alteradas.

Para continuar, abra o app de desenvolvimento em um terminal seu e mantenha-o na porta 9223. Depois me avise; executarei o harness, farei as correções encontradas e, somente se tudo passar, fecharei e commitarei o ciclo.

## você · 2026-08-28 23:56

Pode executar o harnesss

## agente · 2026-08-29 00:13

Sim. O harness foi executado contra o app real.

Resultado:

- 150 cenários funcionais passaram.
- Os 3 cenários de imagens do ciclo 226 passaram.
- Os 9 snapshots de embeds passaram separadamente.
- Corrigi a captura do destino de drop quando as coordenadas apontam para fora do editor.
- Corrigi a fixture temporal do snapshot de cronograma, sem alterar a baseline.

Validações adicionais:

- `cargo test --workspace`: passou, 482 testes.
- `cargo build --manifest-path src-tauri/Cargo.toml`: passou.
- `cargo check` da UI: passou.
- `trunk build`: ainda bloqueado porque falta o target `wasm32-unknown-unknown`.

Por essa pendência obrigatória, mantive o ciclo 226 como `doing` e não criei status `done` nem commit.
