---
title: 2026-08-05
created: 2026-08-05
---

# 2026-08-05

## O que fiz hoje

- Ciclo 002: Vault picker com dialog nativo
  - Criado `crates/vault/` com `VaultIo::list_pages()`
  - `tauri-plugin-dialog` adicionado
  - Botão "Abrir vault" funcional
  - Persistência em localStorage
- Ciclo 003: Sidebar com lista de páginas
  - Comando IPC `list_pages`
  - Componente Sidebar com seções Pages/Journals
  - Destaque visual no item selecionado
  - Empty state para diretórios vazios
- Corrigido bug do dialog: comando customizado `open_vault_dialog`
- Corrigido `Trunk.toml` (target = "index.html")
- Corrigido `dev.sh` (PATH, rustup env, cargo-binstall)
- Gerados ícones RGBA reais

## Aprendizados

- Chamar plugin commands do frontend WASM é frágil
- Melhor criar comandos Tauri customizados que usam a API Rust
- `cargo-binstall` é muito mais rápido que `cargo install`
- `trunk build` usa o `data-target-triple` do HTML, não o `target` do Trunk.toml

## Próximos passos

- Realizar uma analise da possibilidade de criar uma skill para ver o estado de uso dos tokens da sessão no opencode e usar isso para colocar um sleep na execuçao de ciclos para depois que resetar os tokens mantendo o ciclo continuo
- Ciclo 004: Editor Markdown básico
- Criar vault de exemplo pra testar sidebar
