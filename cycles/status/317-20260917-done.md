---
ciclo: "317"
quando: 2026-09-17
resultado: done
---

# 317 — O calendário em modo vault

## Validação

- `cargo test --workspace` — 535 no núcleo (1 novo: página com `date`
  e `time`, página sem data ignorada, `end_date`), 134 na TUI (1 novo:
  calendário vault com o evento, detalhe com a página, Enter devolvendo o
  caminho e selecionando a página; evento escrito não abre nada)
- Os três testes contra o vault real (`--ignored`) passam
- `clippy` sem aviso no código novo; a janela compila com a função movida
- Não conferido no binário real: nenhuma página do vault de exemplo tem
  calendário em `mode: vault`
