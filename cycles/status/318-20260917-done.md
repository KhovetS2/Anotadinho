---
ciclo: "318"
quando: 2026-09-17
resultado: done
---

# 318 — Editar o calendário pela TUI

## Validação

- `cargo test --workspace` — 535 no núcleo, 141 na TUI (7 novos: `o` no
  dia 20 com o evento novo selecionado e o resto do arquivo intacto;
  Escape cancelando; `c` renomeando com o título preenchido e `x`
  apagando; `dd`; `>`/`<` preservando a duração com o cursor seguindo;
  mover pra fora da grade levando a âncora; modo vault recusando)
- Os três testes contra o vault real (`--ignored`) passam
- `clippy` sem aviso no código novo
- Binário real numa CÓPIA do vault (scratchpad), numa aba própria do
  tmux: `o` no dia 20 de agosto, "Teste pela TUI", Enter, `>` — o
  arquivo gravado difere do original só nas duas linhas do evento novo,
  já no dia 21. O vault do repositório não foi tocado.
