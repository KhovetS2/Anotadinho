---
ciclo: "258"
quando: 2026-09-05
resultado: done
---

# 258 — Fechar as specs

Nove specs fechadas. O trabalho não foi mudar `status:` — foi conferir,
uma a uma, se o que a spec pediu está mesmo de pé, e escrever onde.

## O que a conferência achou

Nada faltando, mas duas coisas para registrar em vez de esconder.

A primeira é que os critérios de aceite tinham deixado de valer como
sinal. `tema-configuravel` estava implementada inteira com os seis
critérios em branco; `atalhos-do-dia-a-dia` estava `concluida` com quatro
em aberto. Quem lesse o vault pra saber o que falta erraria nos dois
sentidos. Marquei só o que tem cobertura verde apontável — o critério
"`/` fora da edição abre a busca de comando" eu fui conferir antes de
marcar, e ele tem cenário em `teclado.mjs:230`.

A segunda é que duas specs foram entregues divergindo do que pediram, e
a divergência é boa: o arraste de imagem abre o modal (respondendo a uma
pergunta que a spec deixou em aberto) e o snapshot de tema compara
geometria, não cor. Ficaram escritas nas specs.

## As duas que ficaram abertas

`exemplo-busca-dentro-de-embed` e `exemplo-exportar-nota-em-pdf` não são
backlog: são conteúdo do vault de exemplo, e o estado delas é o dado.
Fechá-las esvaziaria o painel que elas existem pra demonstrar.

## Estado do backlog depois deste ciclo

Nove specs `concluida`, duas de exemplo intactas, `pendentes.mjs` vazio.
Não sobrou spec pedindo trabalho.
