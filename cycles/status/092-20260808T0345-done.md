---
id: "092"
executado_em: 2026-08-08T03:45:00
status: done
agente: claude-sonnet
commit_sha: TBD
testes_total: 96
testes_passaram: 96
testes_falharam: 0
duracao_seg: null
---

# Ciclo 092 - done

## Resumo

Sétimo ciclo do conjunto grande. Substitui o flag `vim_mode` morto por
um motor modal Normal/Insert real, com mapa de teclas configurável e
tela de configuração.

## Arquivos criados/modificados

- `ui/src/state.rs` — `VimKeymap`, `save/load_vim_mode_enabled`,
  `save/load_vim_keymap`
- `ui/src/components/editor.rs` — motor modal (`vim_move`,
  `vim_current_block`, `vim_yank_line`, `vim_delete_line`,
  `vim_paste_after`, `vim_open_line`, `sibling_line_tag`), interceptação
  no `on_keydown`, props `vim_mode_enabled`/`vim_keymap`
- `ui/src/components/vim_settings_modal.rs` — novo
- `ui/src/components/header_bar.rs` — toggle + item de menu
- `ui/src/components/page_view.rs`, `ui/src/app.rs` — threading das
  props/estado
- `ui/src/components/mod.rs`, `ui/src/styles/components.css`

## Testes

Sem testes novos (motor depende de DOM/Selection real, validado ao
vivo — mesma convenção já usada pra outras features baseadas em Range/
Selection nesta sessão). `cargo test --workspace`: 48. `cd ui && cargo
test --lib`: 66. Total 96 combinando os dois runs.

`trunk build` + `cargo build --manifest-path src-tauri/Cargo.toml`: OK.

## Validação ao vivo (MCP tauri)

dd/yy/p/i/a/Esc/Ctrl+S testados com sucesso; bug de `<p>` solto em
listas encontrado e corrigido durante a validação; tela de atalhos
testada (rebind + persistência). Detalhes no arquivo de task.

## Notas

Último ciclo do conjunto grande "planejado" restante: página de Tags,
busca full-text real, undo/redo genérico, gestão de assets.
