---
id: "130"
executado_em: 2026-08-09T07:00:00
status: done
agente: claude-sonnet
commit_sha: TBD
testes_total: 197
testes_passaram: 197
testes_falharam: 0
duracao_seg: null
---

# Ciclo 130 - done

## Resumo

Resolve os dois gaps conscientes do relatório final do ciclo 129:
páginas tipadas (kanban/calendar/table/tags/assets/graph) ganham um
cabeçalho compartilhado com acesso ao painel de Propriedades; e o
`GlobalKeymap` preenche as 10 ações sem tecla padrão com um
mapeamento pensado pra usuário de neovim (referenciando a config real
dele).

## Arquivos criados/modificados

- `ui/src/components/typed_page_header.rs` (novo)
- `ui/src/components/mod.rs` — registra o módulo
- `ui/src/components/page_view.rs` — monta o cabeçalho nos 6 tipos,
  `reload_nonce` pra reroteamento imediato ao mudar `type`
- `ui/src/state.rs` — `GlobalKeymap::default()` com as 10 teclas novas
  + testes atualizados

## Testes

`cd ui && cargo test --lib`: 80 (79→80, teste antigo virou 2 novos).
`cargo test --workspace`: 117. `trunk build` +
`cargo build --manifest-path src-tauri/Cargo.toml`: OK.

Validação ao vivo via MCP `tauri`: botão Propriedades numa página
kanban real, edição de título persistida (corpo intocado, revertido
depois), troca de `type` reroteando na hora numa página descartável;
Ctrl+T/Ctrl+E/Ctrl+D confirmados com o keymap limpo.

## Notas

Ver Notas detalhadas no arquivo de task: processo de decisão das
teclas (o que foi lido da config real de neovim do usuário, o que foi
deliberadamente evitado — a/c/v/x, nativos de cut/copy/paste/select-all
do `contenteditable`), e o achado de teste (keymap customizado antigo
salvo no localStorage da janela de teste, mascarando o novo default
até eu limpar).

Build Windows investigado em paralelo — alvo Rust instalado, toolchain
mingw-w64 pendente de `sudo dnf install` que o usuário está rodando.
Build em si fica pro próximo commit/status, depois que o toolchain
estiver disponível.
