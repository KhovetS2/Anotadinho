---
id: "219"
status: done
---

# 219 — O motivo da falha do agente

Reproduzida a chamada exata que falhou: a conta do Codex bateu o limite
de uso, e o motivo estava no stream. O app mostrava o ruído do stderr.

Ordem de preferência corrigida, evento `error` do Codex tratado, e um
cenário de harness que falha se o ruído voltar a vencer o motivo.

## Validação

- `cargo test --workspace` — 0 falhas (31 testes em `agente`)
- `cargo build --manifest-path src-tauri/Cargo.toml` — 0 erros
- `node scripts/uitest/run.mjs` — **142/142**
