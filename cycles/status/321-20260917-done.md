---
ciclo: "321"
quando: 2026-09-17
resultado: done
---

# 321 — Editar a tabela pela TUI

## Validação

- `cargo test --workspace` — 535 no núcleo, 151 na TUI (3 novos da tabela:
  `c` em texto/select/multiselect com opções novas; `c` no cabeçalho e
  `x`; `o` embaixo da linha e `dd`, com o cabeçalho recusando; mais 1 do
  cronograma do vault só leitura), conferindo o texto em volta do embed
- Os três testes contra o vault real (`--ignored`) passam
- `clippy` sem aviso na TUI
- Na TUI rodando, numa cópia de teste: `c` numa célula e num select
  (opção nova gravada no YAML), `o` + `c`, `dd` — o arquivo mudou só na
  tabela
