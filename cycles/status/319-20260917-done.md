---
ciclo: "319"
quando: 2026-09-17
resultado: done
---

# 319 — Editar o kanban pela TUI

## Validação

- `cargo test --workspace` — 535 no núcleo, 144 na TUI (3 novos: `o` numa
  coluna vazia e `c` no cartão mantendo as tags; `c` na coluna levando os
  cartões junto; `>`/`<` entre colunas com o cursor seguindo, aviso na
  ponta, `x` e `dd`), sempre conferindo que o texto antes e depois do
  embed não mudou
- Os três testes contra o vault real (`--ignored`) passam
- `clippy` sem aviso no código novo
