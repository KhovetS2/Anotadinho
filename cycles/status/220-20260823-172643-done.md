---
id: "220"
status: done
---

# 220 — Atalhos do dia a dia (implementado pelo agente)

Primeiro ciclo implementado de ponta a ponta pelo fluxo agêntico do
próprio Anotadinho: spec → proposta → execução, com o Codex.

## O que ele entregou

- `TabFlag::Home` genérico, home promovida à primeira posição sem
  reordenar as demais
- Fechamento da home bloqueado na UI e na lógica
- "Nova página: Conversa" na família de tipos
- Dois cenários permanentes de harness, migrados de `pendentes.mjs`
- Testes unitários de ordenação e remoção da flag

## Validação

Ele parou antes do status/commit por não conseguir rodar o harness, que
é o que o `AGENTS.md` manda. Rodado aqui, com o app de pé:

- `cargo test --workspace` — 0 falhas
- `cd ui && trunk build` — ok
- `cargo build --manifest-path src-tauri/Cargo.toml` — 0 erros
- `node scripts/uitest/run.mjs` — **144/144**

Conferido na tela: aba inicial primeira, `--fixed`, sem botão de
fechar; demais abas com ×; "Nova página: Conversa" na paleta.
