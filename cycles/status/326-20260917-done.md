---
ciclo: "326"
quando: 2026-09-17
resultado: done
---

# 326 — As colunas lado a lado na TUI

- Referência: print da janela a partir do HTML/CSS reais (`.columns-embed`).
- Testes: núcleo (largura escondida); TUI (lado a lado, `2fr` antes de
  `1fr`, sem `pane`, texto longo quebrado no painel, `h`/`l`).
- Conferido na TUI rodando com o cursor dentro de um painel.
- `cargo test --workspace` e os três testes do vault real passam.
