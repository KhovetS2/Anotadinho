---
ciclo: "320"
quando: 2026-09-17
resultado: done
---

# 320 — Editar o cronograma pela TUI

## Validação

- `cargo test --workspace` — 535 no núcleo, 147 na TUI (3 novos: `o` depois
  da barra do cursor e `c` mantendo as tags; `>`/`<` movendo com a duração
  e `+`/`-` no fim com trava no começo; `x` e `dd`), sempre conferindo que
  o texto antes e depois do embed não mudou
- Os três testes contra o vault real (`--ignored`) passam
- `clippy` sem aviso no código novo
