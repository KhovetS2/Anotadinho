---
id: "095"
executado_em: 2026-08-08T03:00:00
status: done
agente: claude-sonnet
commit_sha: TBD
testes_total: 122
testes_passaram: 122
testes_falharam: 0
duracao_seg: null
---

# Ciclo 095 - done

## Resumo

Décimo ciclo do conjunto grande. Ctrl+Z/Ctrl+Shift+Z desfazem/refazem
qualquer edição (texto solto + mutação de embed) via uma pilha de
snapshots de markdown inteiro por página.

## Arquivos criados/modificados

- `ui/src/components/editor.rs` — `undo_stack`/`redo_stack`/
  `last_content_ref`/`last_snapshot_at`/`render_gen`, empilhamento em
  `mark_edited` (coalescido por tempo), `do_undo`/`do_redo`,
  `Ctrl+Z`/`Ctrl+Shift+Z` no `on_keydown`, reset por página no Effect 1

## Testes

Sem testes novos (depende de DOM/timing real, validado ao vivo).
`cargo test --workspace`: 56. `cd ui && cargo test --lib`: 66. Total
122.

`trunk build` + `cargo build --manifest-path src-tauri/Cargo.toml`: OK.

## Validação ao vivo (MCP tauri)

Undo/redo de texto solto (múltiplos passos) e de mutação de embed
(evento de calendário) confirmados funcionando com o mesmo mecanismo.
Detalhes no arquivo de task.

## Notas

Último item do conjunto grande: gestão de assets.
