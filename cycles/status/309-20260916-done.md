---
ciclo: "309"
quando: 2026-09-16
resultado: done
---

# 309 — Eventos navegáveis, tabela em grade, embed sem rótulo

```
┌────────┬─────────┬─────────────────┐
│ Tarefa │ Status  │ Tags            │
╞════════╪═════════╪═════════════════╡
│ UI     │  doing  │  urgente   bug  │
└────────┴─────────┴─────────────────┘
```

## Dado sem linha ≠ dado que o cursor pula

Até o 308, "não vira linha" e "o cursor não visita" eram a mesma
pergunta (`fica_fora_da_tela`). O evento do calendário separou as duas:
não tem linha, mas é conteúdo. Virou `cursor_passa_por_cima`, e
`Linha::mostra` passou a reconhecer o que está dentro de um segmento.

## Validação

- `cargo test --workspace` — 120 na TUI (9 novos: Enter no dia pulando
  a continuação da sprint, `j`/`k`/Backspace entre eventos, evento
  aceso e visível pra rolagem, grade fechada com réguas alinhadas,
  tabela larga cortando texto, embed aberto sem rótulo, fechado/vazio
  com rótulo, caixa inteira acesa com o cursor no embed; um reescrito:
  o rótulo do callout na cor da variante, agora fechado); núcleo sem
  mudança, 529 verdes
- `clippy` sem aviso no código da TUI
- Binário real numa aba própria do tmux: a tabela da página de exemplos
  em grade, nenhum `[kanban]`/`[table]` nas caixas, e o caminho
  mês → semana → dia 6 → Enter acendendo "Revisão de código" na grade
