---
id: "218"
status: done
---

# 218 — Conversa não afoga o renderizador

## Antes e depois, medido

- Antes: `WebKitWebProcess` a 88,2%, janela sem responder nem a JS,
  sem voltar depois do agente terminar.
- Depois, mesma conversa (31 KB, 16 mensagens): 1–7% em repouso,
  16–18% com trabalho, 0% ao terminar.

## Também neste ciclo

- Baseline do snapshot `query` regravada: a mudança veio do ciclo 217
  (trabalho do agente), e foi conferida na tela antes — alinhamento
  exato entre `th` e `td`, `max-height: 384px` com rolagem, 8 valores
  coloridos em 6 cores.
- `AGENTS.md` reescrito pro que codex e opencode consomem.

## Validação

- `cargo test --workspace` — 0 falhas
- `cd ui && trunk build` — ok
- `cargo build --manifest-path src-tauri/Cargo.toml` — 0 erros
- `node scripts/uitest/run.mjs` — **141/141**
