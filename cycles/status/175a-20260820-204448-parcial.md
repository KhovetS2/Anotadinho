---
id: 175a
status: parcial
---

# 175a — Manipular bloco pelo teclado

## O que mudou

- `ui/src/components/editor.rs`: `AcaoBloco` + `aplicar_acao_de_bloco`,
  ligados ao handler do modo de navegação.
  - `Alt+↑` / `K` — sobe o bloco
  - `Alt+↓` / `J` — desce
  - `y` — duplica logo abaixo
  - `d` — apaga (foco vai pro vizinho de baixo, ou de cima se era o
    último — sem isso o foco cai no `<body>` e as setas param)
  - Depois de reordenar, `marcar_blocos` roda de novo: os
    `data-nav-item` precisam voltar a bater com a posição.
- `scripts/uitest/cenarios.mjs`: cenário cobrindo as quatro ações em
  sequência, o foco preservado e a ordem chegando ao disco.

## Por que age no DOM e não no `Vec<DocSegment>`

Um bloco de texto não é um segmento: é um filho de primeiro nível DENTRO
de um segmento de markdown. Fazer no nível do markdown exigiria mapear
bloco → intervalo de linhas, que é justamente o que o ciclo 176 evitou
ao não escrever id nenhum no arquivo. O DOM já é a fonte de verdade do
texto na gravação (`recompute_markdown_from_dom`), então mover o nó e
recompor é o caminho curto e consistente.

## O que ficou de fora

A reescrita para um `contenteditable` por bloco — ver a seção "Estado:
PARCIAL" na task. Resumo: não é necessária pro que foi pedido, e os
critérios restantes mexem em digitação, o caminho mais usado do app.

## Validação

- `cargo test --workspace`: 0 falhas.
- `trunk build`: `✅ success`; Tauri: 0 erros.
- `node scripts/uitest/run.mjs`: **28/28 em 158.4s**.
