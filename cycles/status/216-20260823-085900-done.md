---
id: "216"
status: done
---

# 216 — Pasta escolhida e execução na mesma conversa

## Verificado com o Codex de verdade

Pedido pela conversa: "Crie o arquivo scratch-prova-216.txt na raiz do
repositório". O arquivo foi criado, com o conteúdo certo. Antes ele
respondia "modo somente leitura".

## Harness

Dois cenários novos (216): executar continua na conversa de origem sem
criar página nova, e as pastas extras chegam como `--add-dir`.

O agente falso ganhou `--args` e passou a consumir os pares
`--add-dir` do começo da linha, como os agentes de verdade.

## Validação

- `cargo test --workspace` — 0 falhas (30 testes em `agente`)
- `cd ui && trunk build` — ok
- `cargo build --manifest-path src-tauri/Cargo.toml` — 0 erros
- `node scripts/uitest/run.mjs` — **140/140**
