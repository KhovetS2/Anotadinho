---
id: "214"
status: done
---

# 214 — Codex, progresso inteiro e troca de agente

## Verificado com o Codex de verdade

Trocado pelo chip da conversa, pergunta enviada, e:

- Progresso ao vivo: "conectado / Vou conferir a pasta `pages`
  diretamente e contar as entradas listadas." — a narração inteira, não
  só a primeira linha.
- Resposta gravada no `.md` pelo backend, com a listagem e "Total: 22".

## Harness

Três cenários novos (214): dialeto do Codex, progresso com mais de uma
linha, e troca de agente preservando o binário ajustado — este último
pegou um bug real na primeira execução.

O agente falso ganhou o modo `--codex`. O cenário do 213 foi ajustado
pro formato novo de linha de ferramenta (`· Read`).

## Validação

- `cargo test --workspace` — 0 falhas (22 testes em `agente`)
- `cd ui && trunk build` — ok
- `cargo build --manifest-path src-tauri/Cargo.toml` — 0 erros
- `node scripts/uitest/run.mjs` — **136/136**
