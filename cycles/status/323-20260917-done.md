---
ciclo: "323"
quando: 2026-09-17
resultado: done
---

# 323 — Editar o callout pela TUI

- 2 testes: título + variante + recolhido; blocos (criar depois de
  parágrafo, `cc` em item mantendo `- `, `o` em item, `dd`, `P`). Achado:
  depois de editar o primeiro item, o cursor ia pra LISTA (começa no mesmo
  byte) — agora vai pro mais fundo.
- Na TUI rodando: `A` no título, `Ctrl+A` (warning → error), `o` no corpo;
  o `diff` mostrou só essas linhas.
