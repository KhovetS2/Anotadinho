---
id: "113"
executado_em: 2026-08-08T08:30:00
status: done
agente: claude-sonnet
commit_sha: TBD
testes_total: 167
testes_passaram: 167
testes_falharam: 0
duracao_seg: null
---

# Ciclo 113 - done

## Resumo

Corrige o botão "+" (Nova página) da sidebar, que tinha implementação
própria e nunca ganhou suporte a templates no ciclo 100 — só o atalho
Ctrl+N e a paleta de comandos (Ctrl+K) tinham o fluxo certo. Bug
encontrado ao validar ao vivo o esquema de agent-os recém-criado no
vault (templates de spec/decisão/padrão/sessão).

## Arquivos criados/modificados

- `ui/src/components/sidebar.rs` — `on_new_page` ganha checagem de
  templates + `PendingDialog::Select`, mesma lógica de
  `new_page_action` (`app.rs`, ciclo 100)

## Testes

`cargo test --workspace`: 92. `cd ui && cargo test --lib`: 75. Total 167.
`trunk build` + `cargo build --manifest-path src-tauri/Cargo.toml`: OK.

Validação ao vivo via MCP `tauri` (com o processo de dev reiniciado do
zero, pra garantir binário atualizado): clicar "+" na sidebar agora
mostra "Escolher template" com as 5 opções + "Página em branco";
escolher "spec" encadeia pro prompt de título; página criada com
`{{title}}`/`{{date}}` resolvidos e item destacado na árvore.

## Notas

Fecha a validação do esquema de agent-os montado nesta sessão
(pastas `pages/produto|specs|decisoes|padroes`, 5 templates, páginas
de exemplo preenchidas, guia em `pages/produto/guia-agent-os.md`).
