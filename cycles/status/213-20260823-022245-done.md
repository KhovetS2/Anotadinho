---
id: "213"
status: done
---

# 213 — Conversa assíncrona

## Verificado com o Claude de verdade, não só com o agente falso

- Disparar voltou em **3ms** (antes prendia a tela até o fim).
- Progresso ao vivo: "conectado / usando Bash" aos 6s.
- Saí da conversa aos 4s; o processo continuou, o backend gravou a
  resposta no `.md`, e voltar mostrou a resposta pronta.
- Interromper: erro "execução interrompida por você", processo do
  agente morto, nada gravado.

## Harness

Seis cenários novos (213): não bloqueia, saída parcial no meio,
interromper mata e reporta, uma execução por conversa, resposta gravada
sem a tela aberta, e stream-json virando progresso + resposta.

O agente falso ganhou dois modos (`--devagar`, `--stream`) — sem eles
não dava pra afirmar nada sobre o MEIO da execução.

O helper `RODAR` dos cenários do 202 foi adaptado: `rodar_agente` deixou
de existir, então ele agora dispara e espera pelo estado.

## Validação

- `cargo test --workspace` — 0 falhas (18 testes em `agente`)
- `cd ui && trunk build` — ok
- `cargo build --manifest-path src-tauri/Cargo.toml` — 0 erros
- `node scripts/uitest/run.mjs` — **133/133 em 348s**
