---
id: "223"
status: done
---

# 223 — Pedir alteração, e os padrões no vault

## Entregue

- Botão "Pedir alteração" em spec/proposta em revisão, abrindo a
  conversa com a pergunta pronta que manda PROPOR
- Sete padrões em `pages/padroes/`: estado em closure, escrita no
  vault, editor e DOM, agente e execução, spec/proposta/execução,
  validação (mais a nomenclatura que já existia)
- Correção da indentação vazando em `pergunta_de_planejamento`, com
  teste que varre todas as perguntas

## Corrigido no caminho

O harness aplicou uma proposta REAL do Codex na spec do usuário, por
clicar no primeiro "Aplicar" da tela. Revertido, devolvido à fila, e os
cenários agora só tocam no item que é deles. Confirmado: a proposta
sobreviveu a uma execução completa da suíte.

## Validação

- `cargo test --workspace` — 0 falhas
- `cd ui && trunk build` — ok
- `cargo build --manifest-path src-tauri/Cargo.toml` — 0 erros
- `node scripts/uitest/run.mjs` — 144/145; a falha conferida isolada e
  passa (timeout intermitente de primeira execução)
