---
ciclo: "308"
quando: 2026-09-16
resultado: done
---

# 308 — Badge por tag e todo embed preenchido

```
▌  Tarefa │ Status  │ Tags            │
▌  UI     │  doing  │  urgente   bug  │
```

## O que só a tela mostrou

Com todo embed preenchido, as bordas dos dias do calendário sumiram:
`--border` (`#3D404F`) e o fundo tingido ficam a poucos pontos de
distância. Nenhum teste pegaria isso — foi a captura colorida do
binário real, convertida em imagem, que mostrou a grade vazia. Virou
`Realce::Grade`, e um teste de DISTÂNCIA entre a grade e o fundo de
cada embed, pra a próxima mudança de cor não repetir.

## Validação

- `cargo test --workspace` — 529 no núcleo (1 novo, 1 reescrito), 114 na
  TUI (5 novos: pílula por tag com fundos próprios, caixa do kanban sem
  vão em volta do cartão, fundo distinto por tipo, grade visível sobre o
  fundo; `dois_embeds_vizinhos_sao_duas_caixas` agora conta `▗`)
- Os três testes contra o vault real (`--ignored`) passam
- `clippy` sem aviso no código novo
- Binário real numa aba própria do tmux, página de exemplos, captura
  colorida em imagem: kanban, calendário, tabela, callout, colunas,
  galeria, consulta, fluxo, cronograma e ações em caixas preenchidas,
  cada uma no seu tom; tags `urgente`/`bug`/`infra` em pílulas azul,
  verde e âmbar
