---
id: "217"
status: done
---

# 217 — Leitura de consultas

## Entregue

- Tabela usa novamente o algoritmo nativo de colunas: `tr` não herda
  mais o `display:flex` da lista.
- `max_height` é opção YAML com padrão de 384 px.
- Badges recebem uma das seis classes de cor a partir de hash FNV-1a
  determinístico de coluna, campo e valor.
- Resultados rolam em área limitada, com cabeçalho sticky e degradê de
  continuação.
- Lista e tabela sem grupos/agregados virtualizam acima de 100 itens,
  com espaçadores, truncamento e foco por seta.
- Revisão: cartão ficou só com o badge (sem a edição em linha do 168,
  que a proposta não pedia), o tooltip do valor passou a mostrar o valor
  inteiro e `.query-settings__hint` ganhou estilo.

## Validação

- `cargo test --workspace` — passou.
- `cargo build --workspace` — passou.
- `cargo build --manifest-path src-tauri/Cargo.toml` — passou.
- `cd ui && cargo check --target wasm32-unknown-unknown` — passou.
- `trunk build` e `scripts/uitest/run.mjs` — bloqueados pela
  incompatibilidade local entre Trunk 0.21.14 e Cargo (`--no-color 1`).
- O `trunk serve` do `cargo tauri dev` que estava rodando recompilou o
  WASM às 14:51 com o código desta revisão até o ajuste dos cartões.
- Os dois últimos ajustes (tooltip e `query-settings__hint`) não foram
  compilados: o `cargo` não estava disponível nesta sessão. Rodar
  `cargo test --workspace` e `cargo check --target wasm32-unknown-unknown`
  antes de commitar.
