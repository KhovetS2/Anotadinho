---
id: "171"
executado_em: 2026-08-21T00:20:00
status: done
agente: claude-opus-5
commit_sha: TBD
testes_total: 284
testes_passaram: 284
testes_falharam: 0
duracao_seg: null
---

# Ciclo 171 - done

## Resumo

A varredura do vault passou a reaproveitar o que já tinha lido: cada
página fica em cache com a marca do arquivo (mtime + tamanho), e só o
que mudou é relido e reparseado. Num vault de 800 páginas: 29,6ms →
4,5ms.

## Arquivos criados/modificados

- `crates/vault/src/index_cache.rs` (novo) — `IndexCache` + 5 testes
- `crates/vault/src/lib.rs` — registro
- `crates/ipc/src/lib.rs` — `handle_scan_vault` usa o cache + 1 teste
- `crates/ipc/examples/bench_scan.rs` (novo) — a medição, versionada
- `.gitignore` — `.anotadinho/`

## Testes adicionados

- cache devolve só quando a marca bate; sobrevive a ida e volta pro
  disco; cache corrompido e de outra versão viram cache vazio; página
  apagada sai do cache
- segunda varredura usa o cache, enxerga mudança no arquivo e some com
  página apagada

## Problemas encontrados

- A task previa SQLite "ao lado do que o search usa" — mas o search usa
  SQLite em MEMÓRIA, não há banco em disco. JSON resolve um mapa path →
  entrada sem peso extra.
- Invalidação ficou pela marca do arquivo, não pelo watcher: pega
  também o que mudou com o app fechado (`git pull`, editor externo).

## Notas para próximos ciclos

- Resta o 176 (id de bloco sob demanda).
