---
id: "109"
executado_em: 2026-08-08T11:30:00
status: done
agente: claude-sonnet
commit_sha: TBD
testes_total: 153
testes_passaram: 153
testes_falharam: 0
duracao_seg: null
---

# Ciclo 109 - done

## Resumo

Pedido direto do usuário: compacta o header do editor (Excluir/⬇
Exportar HTML/🏠 Definir início/Propriedades) num menu "⋯" só, mantendo
"Salvar" como botão principal; painel de propriedades (ciclo 099) vira
um modal em vez de faixa inline colapsável; aba fixa da página inicial
na `TabBar` mostra só o ícone 🏠 em vez do título.

## Arquivos criados/modificados

- `ui/src/components/editor.rs` — header compactado, menu "⋯" (mesmo
  padrão do menu ⚙ da `HeaderBar`), `PropertiesPanel` dentro de `Modal`,
  `is_home` derivado de prop (estado subiu pro `App`)
- `ui/src/components/properties_panel.rs` — remove `<details>/<summary>`,
  vira conteúdo puro pro `Modal`
- `ui/src/components/tab_bar.rs` — prop `home_path`, ícone 🏠 fixo +
  tooltip com o título real
- `ui/src/components/page_view.rs` — repassa `home_page`/`on_toggle_home`
- `ui/src/app.rs` — estado `home_page` + `on_toggle_home` (movidos de
  dentro do `Editor`)
- `ui/src/styles/main.css` — `.header-menu__item--danger`, limpeza do
  CSS órfão de `.properties-panel`/`__summary`

## Testes

`cargo test --workspace`: 82 (inalterado). `cd ui && cargo test --lib`:
71 (inalterado — refactor de UI puro). Total 153.
`trunk build` + `cargo build --manifest-path src-tauri/Cargo.toml`: OK.

Validação ao vivo via MCP `tauri`: aba da home mostra 🏠; header do
editor só com Salvar+⋯; menu lista os 4 itens certos; modal de
propriedades abre com os campos de sempre; exclusão ainda pede
confirmação.

## Notas

Estado "página inicial" precisou subir de `Editor` pro `App` — detalhes
no arquivo de task. Sem regressão de teste porque é puramente estrutural
(mesma lógica, movida de lugar).

Próximo: retomando o tema keyboard-only — ciclo 106 (navegação por
teclado na sidebar).
