---
id: 210
status: done
---

# 210 — Aviso de pendente e execução

## O que mudou

- `crates/core/src/fluxo.rs`: `pergunta_de_execucao`.
- `ui/src/components/header_bar.rs`: indicador de propostas pendentes.
- `ui/src/app.rs`: contagem reconsultada, ação de abrir a revisão,
  escolha entre planejar e executar pelo tipo da página.
- `ui/src/components/embeds/inline_fluxo.rs`: o botão vira "Executar"
  numa proposta aprovada.
- `ui/src/components/propostas_view.rs`: avisa quando a fila muda.
- `ui/src/components/conversa_view.rs`: promover em execução.

## Ciclo completo, agora

spec (o quê) → aprovada → **Planejar** → proposta (o como) → aprovada →
**Executar** → execução (o registro). Cada seta exige um clique humano.

## Validação

- `cargo test --workspace`: 0 falhas.
- `node scripts/uitest/run.mjs`: **127/127 em 357.1s**.
- Ao vivo: proposta criada pelo CLI fez o aviso aparecer; clicar levou à
  revisão; recusar zerou o aviso; e a proposta aprovada abriu a conversa
  de execução com a pergunta certa.
