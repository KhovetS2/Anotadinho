---
id: "222"
status: done
---

# 222 — Travamento do compositor com driver NVIDIA

Causa achada por amostragem de pilha: o processo travado está sempre em
`BitmapTexturePool::releaseUnusedTexturesTimerFired()` dentro do
`libnvidia-eglcore`. Backend em 0%, sem IPC, sem JS na pilha.

`WEBKIT_DISABLE_DMABUF_RENDERER=1` definido no `main()`, antes do
WebView subir. Confirmado que chega ao `WebKitWebProcess`.

## Validação

- `cargo test --workspace` — 0 falhas
- `cargo build --manifest-path src-tauri/Cargo.toml` — 0 erros
- `node scripts/uitest/run.mjs` — 142/144; as duas falhas conferidas
  uma a uma e ambas ambientais (uma instabilidade de primeira execução,
  que passa isolada; e o snapshot acusando largura de janela)
