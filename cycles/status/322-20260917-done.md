---
ciclo: "322"
quando: 2026-09-17
resultado: done
---

# 322 — Editar embeds pela gramática do vim

## Validação

- `cargo test --workspace` — 537 no núcleo (2 novos da gramática: `>>`,
  `<<` e `Ctrl+A/X` com contagem; a tabela `edicao_de`), 161 na TUI (os
  testes do 318–321 passaram a usar `a`, `>>`, `Ctrl+A`; novos: `O` e `cc`,
  `yy`/`dd` + `p`/`P` no kanban, `2>>` + `u` + `Ctrl+R`, comandos fora de
  embed sem efeito, `O`/colar no cronograma, `Ctrl+A` no select e `yy p` na
  tabela, `Ctrl+A` e `yy p` no calendário)
- `cargo check --target wasm32-unknown-unknown` na `ui` passa
- Na TUI rodando, numa cópia: `u` três vezes voltou o arquivo idêntico ao
  original (`diff` vazio), `Ctrl+R` refez
